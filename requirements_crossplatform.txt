# Cross-Platform Hackathon Monitor Requirements
# Works on Windows, macOS, and Linux

# Core dependencies (all platforms)
requests>=2.31.0
beautifulsoup4>=4.12.0
lxml>=4.9.0
openpyxl>=3.1.0
schedule>=1.2.0
selenium>=4.15.0
webdriver-manager>=4.0.0
python-dateutil>=2.8.0
configparser>=5.0.0
Pillow>=10.0.0

# GUI dependencies (all platforms)
# tkinter is included with Python, no need to install

# Windows-specific notifications
win10toast>=0.9; sys_platform == "win32"
pywin32>=307; sys_platform == "win32"

# macOS-specific notifications  
pync>=2.0.3; sys_platform == "darwin"

# Linux-specific notifications
plyer>=2.1.0; sys_platform == "linux"
notify2>=0.3.1; sys_platform == "linux"

# Web interface dependencies
Flask>=2.3.0

# Terminal UI dependencies (optional)
rich>=13.0.0

# Optional: Enhanced cross-platform notifications
# Uncomment if you want additional notification options
# desktop-notifier>=3.5.0
# winotify>=1.1.0; sys_platform == "win32"
